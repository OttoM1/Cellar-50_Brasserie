{
  "name": "cellar50-brasserie",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build:css": "sass styles/main.scss components.css --style=expanded",
    "watch:css": "sass styles/main.scss components.css --watch",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
